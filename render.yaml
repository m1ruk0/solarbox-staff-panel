services:
  - type: web
    name: staff-management-panel
    env: node
    buildCommand: npm install
    startCommand: node api-server-mongo.js
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: API_PORT
        value: 4000
